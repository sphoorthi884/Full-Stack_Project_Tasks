Task 6 — Full Stack Mini Project (Front-end + Express Server)
------------------------------------------------------------

How to run:
1. Open terminal inside Task_6
2. npm install
3. npm start
4. Open http://localhost:3000 in your browser

What it includes:
- Full-stack structure combining a Node.js/Express backend with a simple front-end
- Server routes for handling requests and sending responses
- Front-end UI (HTML/CSS/JS) connected to the backend
- Example of end-to-end data flow between client and server
- In-memory logic that can be replaced with a real database

Features:
- Express server setup with organized routes
- Front-end form/UI that interacts with the backend
- JSON-based request and response handling
- Clean project folder structure

Notes:
- This project demonstrates how a front-end communicates with a backend API.
- Ideal for beginners to understand full-stack flow.
- Can be extended with MongoDB, authentication, templates (EJS), or advanced UI.
