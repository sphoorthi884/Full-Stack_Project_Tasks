<!DOCTYPE html>
<html>
<head>
  <title>Submission Successful</title>
  <style>
    body { font-family: Arial; padding: 30px; background: #f9f9f9; }
    .box { background:white; padding:20px; max-width:450px; margin:auto; border-radius:10px; }
  </style>
</head>

<body>

  <div class="box">
    <h1>Submission Received</h1>
    <p><b>Name:</b> <%= record.name %></p>
    <p><b>Email:</b> <%= record.email %></p>
    <p><b>Age:</b> <%= record.age %></p>
    <p><b>Message:</b> <%= record.message %></p>

    <h2>All Submissions</h2>
    <ul>
      <% submissions.forEach(s => { %>
        <li><%= s.id %> — <%= s.name %> — <%= s.email %></li>
      <% }) %>
    </ul>

    <a href="/">Back to form</a>
  </div>

</body>
</html>
